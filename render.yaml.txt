services:
  - type: web
    name: eco-city-rubtsovsk
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python init_db.py          # ← эта команда выполнит инициализацию БД при деплое
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        generateValue: true
    healthCheckPath: /
    autoDeploy: true

databases:
  - name: eco-city-db
    databaseName: eco_city
    user: eco_city_user
    plan: free
    region: ohio
    ipAllowList: []