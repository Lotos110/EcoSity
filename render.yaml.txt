services:
  - type: web
    name: eco-city-rubtsovsk
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        generateValue: true
    healthCheckPath: /
    autoDeploy: true

databases:
  - name: eco-city-db
    databaseName: eco_city
    user: eco_city_user
    plan: free
    region: ohio  # или frankfurt, oregon, singapore и т.д. – выберите тот же, что и у веб-сервиса
    ipAllowList: []  # разрешить подключения только из внутренней сети Render (безопасно)